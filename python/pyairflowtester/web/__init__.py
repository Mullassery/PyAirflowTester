"""Optional web dashboard for PyAirflowTester's dependency intelligence graph.

This package is only importable if the optional `web` extra is installed:

    pip install pyairflowtester[web]

It has no effect on (and is not imported by) the core CLI/scan/analysis path.
"""
